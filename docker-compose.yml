services:
  popg-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - DATA_DIR=/app/data
      - OLLAMA_URL=http://host-gateway:11434
      - OLLAMA_MODEL=llama3
      - WHISPER_MODEL=small
      - WHISPER_DEVICE=cuda
    volumes:
      - ./data:/app/data
      - ./data/whisper-cache:/root/.cache/whisper
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
